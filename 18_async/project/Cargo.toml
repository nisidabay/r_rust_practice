[package]
name = "fetch_multi"
version = "0.1.0"
edition = "2021"

[dependencies]
# ureq is a blocking HTTP client — we use it with thread::spawn
# to demonstrate concurrent fetching without a full async runtime
ureq = "2"

[dev-dependencies]
tempfile = "3"